## 2026-04-23

- CodeRabbit review was unusually slow and required multiple wait cycles before the first actionable review appeared.
- PR merge completed before the explicit `gh pr merge` call; final merge metadata had to be read from `gh pr view` instead of the merge command output.
